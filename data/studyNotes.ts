import { SecurityDomain } from '../types';

export const STUDY_NOTES: Record<SecurityDomain, string> = {
  general_security_concepts: `CIA triad (confidentiality, integrity, availability), non-repudiation, AAA (authentication/authorization/accounting), the four control types (technical, managerial, operational, physical), and Zero Trust ("never trust, always verify").`,

  threats_vulnerabilities_mitigations: `Threat actors range from unsophisticated "script kiddies" using pre-built tools, to organized crime motivated by financial gain, to nation-state actors with the most resources and patience, to insider threats who already have legitimate access. Attack surface is every point where an attacker could get in — reducing it (closing unused ports, removing unused accounts, patching software) is one of the cheapest, highest-impact defenses.

Common attack types: phishing (broad, untargeted fraudulent messages) versus spear phishing (targeted at a specific person using personal details) versus whaling (targeted at executives). Malware categories: viruses (need a host file, spread when that file runs), worms (self-replicating, no host needed), ransomware (encrypts data and demands payment), and trojans (disguised as legitimate software). On the network side, a man-in-the-middle attack intercepts communication between two parties who believe they're talking directly to each other, while a denial-of-service attack overwhelms a system so legitimate users can't use it.

Vulnerabilities show up in different forms too: a zero-day is a flaw with no available patch yet, which is why defense-in-depth (layered controls, not one single fix) matters so much. Mitigation techniques include patch management (keeping software updated on a schedule), segmentation (isolating systems so a breach in one area doesn't spread), and least privilege (giving accounts only the access they strictly need, nothing more).`,

  security_architecture: `Architecture is about how systems are designed and connected, not just what tools are bolted on afterward. Network segmentation splits a network into smaller zones (like putting guest WiFi on its own segment, separate from internal servers) so a compromise in one zone doesn't automatically expose everything else. A demilitarized zone (DMZ) sits between the public internet and an internal network, hosting things like public web servers so they're reachable without directly exposing internal systems.

Cloud concepts matter here too: the shared responsibility model means the cloud provider secures the underlying infrastructure, while the customer is still responsible for securing their own data, configurations, and access — a common source of real-world breaches is assuming the provider handles everything. High availability design (redundant servers, failover systems, load balancing) keeps critical systems running even if one component fails, tying back to the "availability" leg of the CIA triad.

Encryption fits into architecture as a design decision, not an afterthought: data at rest (stored on disk) needs different protection than data in transit (moving across a network), and both need protection distinct from data in use (actively being processed in memory). Public key infrastructure (PKI) uses key pairs — a public key anyone can have, and a private key kept secret — to enable both encryption and digital signatures, forming the backbone of things like HTTPS.`,

  security_operations: `This is the day-to-day discipline of actually running a secure environment. Identity and access management (IAM) covers how accounts get created, what they're allowed to do, and how that access gets revoked when someone leaves — a huge number of real breaches trace back to old accounts that were never deactivated. Multi-factor authentication (MFA) requires proving identity through more than one category: something you know (a password), something you have (a phone or hardware token), or something you are (a fingerprint).

Vulnerability management is an ongoing cycle, not a one-time scan: identify vulnerabilities (scanning tools), prioritize them (not every finding is equally urgent — a critical flaw on an internet-facing server matters more than a minor one on an isolated internal machine), remediate (patch, reconfigure, or accept the risk with justification), and verify the fix actually worked.

Incident response follows a standard lifecycle: preparation (having a plan before anything happens), detection and analysis (figuring out something is actually wrong), containment (stopping it from spreading further), eradication (removing the actual cause), recovery (getting systems back to normal safely), and lessons learned (updating the plan so the same thing is handled better next time). Logging and monitoring (via a SIEM — security information and event management system) is what makes detection possible in the first place; you can't respond to what you never noticed.`,

  security_program_management: `This domain is about governance — the policies, risk decisions, and organizational structure that everything else operates inside of. Risk management starts with identifying assets and threats, then assessing likelihood and impact, and choosing a response: accept the risk (it's minor or too costly to fix), avoid it (stop the risky activity entirely), transfer it (insurance, or a third-party contract), or mitigate it (reduce likelihood or impact with controls).

Security policies formalize expectations — an acceptable use policy defines what employees are and aren't allowed to do with company systems, while a data classification policy defines sensitivity tiers (public, internal, confidential, restricted) so people know how carefully to handle different types of information. Third-party risk management matters because an organization's security is only as strong as its weakest vendor — a breach at a supplier with network access can become the organization's own breach.

Compliance frameworks (like NIST CSF, ISO 27001, or industry-specific rules like HIPAA for healthcare or PCI DSS for payment card data) exist because certain industries have legal or contractual obligations to meet a minimum security bar, and audits verify that the organization is actually meeting it, not just claiming to.`,
};
